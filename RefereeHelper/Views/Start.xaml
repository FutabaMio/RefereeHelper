<UserControl x:Class="RefereeHelper.Views.Start"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RefereeHelper.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="White">
        <Grid.RowDefinitions>
            
            <RowDefinition Height="367*"/>
            <RowDefinition Height="48*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Name="manualAddStart" Content="Добавить вручную" FontSize="20" Click="manualAddStart_Click"/>
            <Button Grid.Column="1" Name="editStart" Content="Редактировать" FontSize="20" Click="editStart_Click"/>
            <Button Grid.Column="2" Name="deleteStart" Content="Удалить" FontSize="20" Click="deleteStart_Click"/>
        </Grid>
        <DataGrid Grid.Row="0" Name="startTable" ItemsSource="{Binding}"/>
    </Grid>
</UserControl>
